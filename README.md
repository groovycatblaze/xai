# XAI for Text (Thinking Machines)
Hugging Face sentiment model + LIME explanations. Streamlit UI included.

## Quickstart (CLI)
python -m venv .venv && source .venv/bin/activate
pip install --upgrade pip && pip install -r requirements.txt
python src/explain_text.py

## Streamlit Demo
streamlit run app.py
